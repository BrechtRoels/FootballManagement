# Base URL the frontend uses to reach the API.
# Leave as /api in development to use Vite's dev proxy (see vite.config.ts).
# In production set this to your deployed backend, e.g. https://api.yourclub.com/api
VITE_API_URL=/api
